import 'package:orzu_grand/all_pages.dart';

class CartPage extends StatelessWidget {
  const CartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(KTStrings.korzina,style: TextStyle(fontSize: 30.sp),),
    );
  }
}
